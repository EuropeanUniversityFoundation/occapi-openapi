type: object
description: JSON:API _Organisational Unit_ resource.
required:
  - data
properties:
  jsonapi:
    $ref: ./links/Jsonapi.yaml
  data:
    type: object
    description: _Organisational Unit_ resource data.
    required:
      - type
      - id
      - attributes
      - links
    properties:
      $ref: ./OunitData.yaml
  links:
    type: object
    description: JSON:API _links_ object.
    properties:
      self:
        $ref: ./links/LinksResource.yaml
